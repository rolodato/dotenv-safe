{
  "name": "dotenv-safe",
  "version": "2.2.0",
  "description": "Load environment variables from .env and ensure they are defined",
  "main": "index.js",
  "scripts": {
    "test": "set -e && cd test && node **.js"
  },
  "keywords": [
    "dotenv"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/rolodato/dotenv-safe.git"
  },
  "author": "Rodrigo López Dato <rlopezdato@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^1.2.0"
  },
  "devDependencies": {
    "assert": "^1.3.0"
  }
}
